[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Redirect API calls to backend 
[[redirects]]
  from = "/api/*"
  to = "https://cyber-sentinel-ai-1.onrender.com/api/:splat"
  status = 200
  force = true
